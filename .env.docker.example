# ========================================
# Docker Compose Environment Variables
# ========================================
#
# 이 파일을 복사하여 .env 파일을 생성하세요
# cp .env.docker.example .env
#
# ⚠️  .env 파일은 Git에 커밋하지 마세요!

# ========================================
# Application Environment
# ========================================
NODE_ENV=production

# ========================================
# PostgreSQL Database
# ========================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-this-strong-password
POSTGRES_DB=election_db
POSTGRES_PORT=5432

# ========================================
# Backend Configuration
# ========================================
BACKEND_PORT=3000

# JWT Secret (64자 이상 강력한 랜덤 문자열)
# 생성: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-64-characters

# Ballot Secret Salt (64자 이상 강력한 랜덤 문자열)
# 생성: openssl rand -base64 64
BALLOT_SECRET_SALT=your-super-secret-ballot-salt-change-this-in-production-min-64-characters

# CORS Origins (쉼표로 구분)
CORS_ORIGIN=http://localhost:3001,https://yourdomain.com

# ========================================
# Frontend Configuration
# ========================================
FRONTEND_PORT=3001

# API URL (프론트엔드에서 백엔드 호출 시 사용)
NEXT_PUBLIC_API_URL=http://localhost:3000

# ========================================
# Nginx (Optional)
# ========================================
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# ========================================
# Email Service (Optional)
# ========================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# EMAIL_FROM="선거시스템 <noreply@yourdomain.com>"
