/**
 * Jest Setup File
 *
 * 각 테스트 파일이 실행되기 전에 실행됩니다.
 */

// 테스트 타임아웃 설정
jest.setTimeout(10000);

// 콘솔 로그 최소화 (필요시 주석 해제)
// global.console = {
//   ...console,
//   log: jest.fn(),
//   debug: jest.fn(),
//   info: jest.fn(),
//   warn: jest.fn(),
//   error: jest.fn(),
// };
